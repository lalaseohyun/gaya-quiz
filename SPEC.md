# 가야면 실시간 퀴즈 웹앱 — 개발 지시서

정읍 SDGs 원탁회의 퀴즈앱과 **같은 구조·같은 포맷**으로 만듭니다.
달라지는 건 팀 수(10 → 4)와 문항 데이터뿐입니다.

---

## 1. 기본 정보

| | |
|---|---|
| 행사 | 가야면 1차 주민워크숍 |
| 문항 | 10문항 (연습문제 없음, 전부 점수 반영) |
| 보기 | 문항당 3개 |
| 팀 | **4팀 · 이름 없이 1번 / 2번 / 3번 / 4번** |
| 배점 | 1~8번 100점 · **9~10번 200점** (총 1,200점) |
| 데이터 | `data/gaya-quiz-2026.json` |

---

## 2. 화면 구성

### 진행자 화면 `/host?k=gaya2026`
- 빔프로젝터 투사용. 글자 크게
- 문항 번호 / 남은 팀 표시 / 문항 / 보기 3개
- **[정답 공개]** 버튼 → `highlight`를 크게 띄우고, 팀별 정오 표시
- `explanation`은 화면에 띄우지 않음 — 진행자가 말로 함 (별도 진행노트 참조)
- 하단에 팀별 누적 점수 바
- **[다음 문항]** / **[처음으로]** (관리자 비번)

### 팀 화면 `/play?k=gaya2026`
- 첫 화면에서 **1~4번 중 자기 팀 선택**
- 문항이 열리면 보기 3개가 큰 버튼으로 표시
- **팀당 1회만 제출**, 제출 후 잠금
- 제출하면 "제출됐습니다" + 팀 번호 표시
- 진행자가 공개하면 정오 결과 표시

### 결과 화면
- 팀별 누적 점수 순위 (4팀)
- 문항별 정답률 — **진행자가 어느 장을 더 짚을지 판단하는 자료**

---

## 3. 데이터 구조

```json
{
  "meta": { "teamCount": 4, "teamLabels": ["1번","2번","3번","4번"], "answerIndexBase": 0 },
  "questions": [
    {
      "no": 1,
      "scored": true,
      "points": 100,
      "sheet": 1,
      "question": "가야면에 사는 사람은 몇 명일까요?",
      "options": ["3,100명", "3,500명", "3,900명"],
      "answerIndex": 2,
      "highlight": "3,863명",
      "explanation": "…"
    }
  ]
}
```

- `answerIndex`는 **0부터** 셉니다. 위 예시의 정답은 3번 보기입니다
- `highlight` — 정답 공개 화면에 크게 띄울 한 줄
- `explanation` — 진행자가 말로 할 내용. 화면에 안 띄움
- `sheet` — 배포자료 몇 번째 장과 연결되는지. 결과 화면에서 "3장을 더 보세요" 안내에 사용

**정답 배열** ③①③②②①①②③②

---

## 4. 디자인

라인교육연구소 시스템을 따릅니다. 배포자료 A3와 같은 톤.

| 용도 | 값 |
|---|---|
| 본문·제목 | `#3A3A38` |
| 배경 박스 | `#F4F3EE` |
| 강조 | `#FDCE0F` |
| 화면 배경 | `#FFFFFF` |

- 강조는 **글자 뒤 노란 띠**로. `linear-gradient(transparent 56%, #FDCE0F 56%, #FDCE0F 95%, transparent 95%)`
- 정답 표시는 노랑, 오답은 회색. **빨강·초록 쓰지 않음**
- 폰트 SB어그로 / KoPubWorld, 없으면 Pretendard
- 진행자 화면 문항 48pt 이상, 보기 36pt 이상 — 뒷자리에서 읽혀야 함
- 팀 화면 보기 버튼은 손가락으로 누르기 쉽게 최소 높이 64px

---

## 5. 배포 — 고정 주소

임시 터널(trycloudflare) 쓰지 않습니다. 주소가 바뀌면 QR이 죽습니다.

- **GitHub Pages** — `lalaseohyun.github.io/gaya-quiz`
- **Firebase Realtime Database** — 실시간 동기화

### Firebase 규칙

```json
{
  "rules": {
    ".read": true,
    ".write": true,
    "rooms": {
      "$room": {
        "answers": { ".indexOn": "team" }
      }
    }
  }
}
```

> **`now < timestamp` 형태로 쓰지 마세요.** 만료되면 앱이 통째로 멈춥니다.

### DB 구조

```
rooms/gaya2026/
  state/     currentQuestion, revealed, phase
  answers/   {questionNo}/{teamNo} = optionIndex
  scores/    {teamNo} = 누적점수
```

---

## 6. 진행 흐름

```
진행자: 문항 띄우기
   ↓
팀: 1~4번 중 하나 선택 → 제출 (팀당 1회)
   ↓
진행자: [정답 공개] → highlight + 팀별 정오
   ↓
진행자가 말로 해설 → [다음 문항]
   ↓
10문항 끝 → 팀별 순위 + 문항별 정답률
```

---

## 7. 반드시 챙길 것

- **동시 제출** — 4팀이 같은 순간 눌러도 다 들어와야 함
- **새로고침해도 팀 선택 유지** — localStorage 아닌 URL 파라미터나 DB로
- **관리자 초기화** — 리허설 후 점수 리셋. 비번 걸어둘 것
- **오프라인 대비** — 마을회관 와이파이가 약할 수 있음. 팀 대표 휴대폰 데이터 접속을 기본으로 안내
- **종이 백업** — 앱이 죽어도 진행되도록 10문항 A4 인쇄본 준비

---

## 8. 문항 성격 참고

| 번호 | 의도 |
|---|---|
| 1~2 | 자부심으로 열기. 2번 해설에서 1위 여덟 개가 쏟아짐 |
| 3 | 인구 감소 — 분위기가 가라앉는 지점 |
| **4** | **반전.** 빈집이 합천에서 가장 적음 |
| 5 | 불편 — 버스 없는 마을 8곳 |
| **6** | **핵심.** 가야면과 야로면은 같은 북부인데 할 일이 다름 |
| 7 | 받아온 것 확인 |
| **8** | **주민 설문 56.5%와 연결.** 남의 통계가 우리 이야기가 되는 지점 |
| 9~10 | 배점 2배. 순위가 뒤집히는 구간 |

**9·10번은 다 틀리는 게 정상입니다.** "적게 받은 게 아니다", "스무 가지가 적혀 있는데 하나도 안 정해졌다" — 이 두 사실이 퀴즈 끝에 남으면 배포자료를 펴기 전에 이미 오늘의 이유가 섭니다.
